**Solution Runtime:** `0ms` (Beats `100%`)
**Solution Memory:** `19.26mb` (Beats `73.15%`)

These are basically meaningless. Everyone has the `O(1)` solution, and are so close
it's just down to instance performance.

**Thoughts**

I wouldn't have bothered doing this problem, except the comment about an O(1) 
solution caught my eye. Overall, still easy but more satisfying than just solving 
the problem the naive way. 
**Solution Runtime:** `100ms` (Beats `36.35%`)
**Solution Memory:** `62.31mb` (Beats `65.19%`)



**Thoughts**

Algorithm is a straightforward iteration through a linked list, just at two different
paces. The optimizations to lower runtime weren't particularly interesting to me.

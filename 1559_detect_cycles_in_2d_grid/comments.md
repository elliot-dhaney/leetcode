**Solution Runtime:** `443ms` (Beats `41.32%`)
**Solution Memory:** `66.28mb` (Beats `86.53%`)


**Thoughts**

Kind of an annoying form for a graph to take, but the actual algorithm was 
a standard graph traversal. I ran into a little issue using the wrong set method 
and not actually updating my `allSeen` dict, but otherwise wasn't very hard.